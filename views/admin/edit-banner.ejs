      <!-- Breadcrumb Section Begin -->

      <section class="breadcrumb-option">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="breadcrumb__text">

                        <h2 class="text-center font-weight-bold" style="color: rgba(22, 22, 22, 0.751);">Admin
                            Dashboard
                        </h2>

                    </div>
                </div>
            </div>
        </div>
    </section>
    <div class="container mt-5">
        <h2>Edit Bannner</h2>
        <form action="/admin/edit-banner/<%= banner._id %>" method="post" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="name" class="form-label">Banner Name</label>
                <input type="text" class="form-control" id="name" name="name" value = "<%= banner.name %>" required>
            </div>
            <div class="mb-3">
                <label for="name" class="form-label">Banner Description</label>
                <input type="text" class="form-control" id="description" name="description" value = "<%= banner.description %>" required>
            </div>
            <div class="mb-3">
                <label for="image" class="form-label">Banner Image</label>
                <!-- Display the current brand image -->
                 <img src="/upload/banner/<%= banner.image %>" alt="Banner Image" class="img-fuild">
                <input type="file" class="form-control " name="image" value=""  >
            </div>
            <div class="mb-3 form-check">
                <input type="checkbox" class="form-check-input" id="active" name="active" value="on"<% if (banner.active) { %>checked<% } %>>
                <label class="form-check-label" for="active">Active</label>
            </div>
            <button type="submit" class="btn btn-primary">Update Banner</button>
        </form>
    </div>